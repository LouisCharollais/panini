{% extends 'base.html.twig' %}

{% block title %}Hello_World{% endblock %}
{% block body %}
    {{ parent() }}
    <div class = "container">
        <h1>{{ panini.nom }}</h1>
        <div class = "row">
            <div class = "col-md-12">
                <a href="{{ path('equipe_show', { 'id': equipe.id}) }}">Retour vers l'équipe</a>
            </div>
        </div>
    </div>
{% endblock %}