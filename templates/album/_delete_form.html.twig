<style>
    .delete-form {
        margin-top: 20px;
        text-align: center;
    }

    .delete-form button {
        background-color: #dc3545;
        color: #fff;
        border: none;
        padding: 10px 20px;
        cursor: pointer;
        border-radius: 5px;
        font-size: 16px;
    }

    .delete-form button:hover {
        background-color: #c82333;
    }
</style>

<form class="delete-form" method="post" action="{{ path('album_delete', {'id': album.id}) }}" onsubmit="return confirm('Etes-vous sûr de supprimer cet album?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ album.id) }}">
    <button class="btn">Supprimer</button>
</form>